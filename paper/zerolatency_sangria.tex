\section{Applying the Premerger Search}
\label{sec:zerolatency_sangria}

\iwh{ Things I think should appear here:
\begin{itemize}
    \item A description of the dataset and PSDs used, including Figure 1 ... I actually wonder whether this should be its own section as it's nothing to do with the zero-latency filter.
    \item Results of running the zero-latency search on the dataset. The overly-optimistic (ie. no GBs) model PSD is probably not going to give meaningful output, but the estimated PSD should be close enough to the model PSD + WD background. A table of how far before merger we find each of the signals with high significance might be useful.
    \item I would blank out the region of 4 signals for now and just say that we'll discuss this special case later and then go straight into inpainting.
\end{itemize}}

Our search uses a zero-latency filter such that data from the future is not required. A full
description is in~\cite{CabournDavies:2024hea}, and is based on work from~\cite{Tsukada:2017cuf}.

To apply this search, we set up the data so that we can truncate it, this was done in hourly intervals,
and then apply the search at 14, 7, 4 and 1 days before merger.


\subsection{Secondary Peaks in Signal-to-Noise Ratio}
\label{subsec:double_peaks}
\gscd{Discussion of finding the secondary peaks in the SNR.}

\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{images/zero_latency_results/figure_X_signal_6_raw.pdf}
    \caption{
        Signal 6 results, showing the secondary peaks caused by overlap between the merger and the pre-merger 
    }
    \label{fig:zerolatency_signal_six_raw}
\end{figure}

\subsection{Secondary Peak Removal}
\label{subsec:peak_removal}
\gscd{Removing the peaks, and the results of the search including removing the peaks. Discussion of whether assuming that the signal is known is valid or not}

Signal 1 appears to now be missed in the plot, however the former peak in SNR is from the 0.5 days post merger analysis
matching to the peak itself.
Signal 5 can now be seen, as the contamination from the louder Signal 4 is now no longer a problem.

\gscd{Discussion of how we remove the secondary peaks in the SNR.}

\begin{figure}
    \centering
    \includegraphics[width=\columnwidth]{images/zero_latency_results/figure_X_signal_6_removed.pdf}
    \caption{
        Signal 6 results, showing the secondary peaks removed as the signal is removed just before merger
    }
    \label{fig:zerolatency_signal_six_removed}
\end{figure}

\subsection{Search Results}
\label{subsec:zerolatency_results}
\gscd{Discussion of the analysis results}

\begin{figure*}
    \centering
    \includegraphics[width=\textwidth]{images/zero_latency_results/psd_model/figure_X_zerolatency_result.png}
    \caption{
        Search Results for the Zero Latency Search
    }
    \label{fig:zerolatency_remove_all}
\end{figure*}
